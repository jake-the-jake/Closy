#pragma once

namespace closy {

class Avatar;
class Scene;

/** Default shirt (torso + sleeves) + split trousers; matches viewport / export outfit. */
void attachDemoOutfit(Scene& scene, Avatar* avatar);

} // namespace closy
