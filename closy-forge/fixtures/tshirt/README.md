# T-Shirt Fixtures

Implementation 01 generates the deterministic T-shirt from `closy_forge.garments.tshirt`. This directory is reserved for small authored fixture notes only; generated `.closygarment` packages are intentionally ignored.
