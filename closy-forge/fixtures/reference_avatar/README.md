# Reference Avatar Fixtures

Implementation 01 generates the synthetic reference avatar from `closy_forge.avatar.reference_avatar` during package builds. No external avatar binary or user-identifying data is checked in here.

Golden fixture data is structural and lives under `tests/golden/`.
