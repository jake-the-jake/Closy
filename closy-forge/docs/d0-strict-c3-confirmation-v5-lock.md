# D0 strict C3 confirmation v5 lock

Unit N repairs and version-locks the strict C3 evaluator; it does not change the Unit F
candidate, topology, binding strategy, binding weights, runtime, fallback, or thresholds.

The deterministic sentinel rule resolved the exact Unit F candidate because Unit M ended in
`attempted_integrity_error`. H4 remains consumed at 0/8 and cannot be replayed for qualification.

The committed lock contains eight semantic pose classes and their supported ranges but no realized
pose parameters, seed, target, or target hash. The first external authority run will accept one
random 256-bit seed, publish pose/target commitments, execute once, reveal the synthetic targets,
and consume the attempt regardless of pass, numeric failure, or post-commitment integrity failure.

The independent oracle does not import or read candidate binding weights and does not call candidate
reconstruction. It is project-authored analytic synthetic evidence that shares the frozen pose
semantics; it is not physical cloth or real-world deformation ground truth.

Z1 and MT1 remain `not_run/dependency_blocked` because no matching authenticated executable is
available. This optional evidence cannot stall Unit O.

The sole fresh authority run `33546821637`, job `99986277154`, consumed the attempt at exact head
`d7b6e810477f169fea3a3cfca23c5ed99ba603b7`. All eight committed classes passed: maximum binding
reconstruction error was `6.71791165111579e-08 m`, seam crack and tangential sliding were `0 m`,
inverted triangles were `0`, maximum whole-evaluation time was `0.505678384000007 s/pose`, and
maximum peak memory was `4.04457187652588 MiB`. This closes D0-RP-08 only for the exact synthetic,
pre-topology Unit F sentinel. It does not establish physical-cloth or real-world deformation truth,
and any topology change requires a new confirmation.
