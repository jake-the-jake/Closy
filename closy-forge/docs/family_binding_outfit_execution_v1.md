# Family / binding / outfit integration execution

## Dependency map and protected checkpoint

Base: PR66 `930b3da556c96e9ded52b6ee8df5620d4903c280`, tree
`1aee06f2d65bd66a08c63e30ad47ceb65c85a590`. Verified open/draft; run
33941010799 has 32 successful Forge jobs and a separate skipped Supabase check.
Main remains `859d4ee9a8a3386e95ec8c29043aa9ecc246769a`.

1. A: frozen panel builders -> guarded numerical settling -> subdivision/binding ->
   decoded conventional validation -> 27 configurations in two clean roots ->
   all-family source/panel round trips and generated index.
2. B: actual PR65 residual localization -> versioned geometric binding -> serialized
   independent reconstruction -> 99 exposed baseline states and extra variations.
3. C: A simulation packages + B-compatible bindings -> geometric package layering ->
   runtime V3 trusted identities/prefix decode -> optional read-only ZeroOne -> demo.

Use one heavyweight evaluator at a time. Pure tests/report parsing may run alongside.
No historical evaluator reruns, snapshot regeneration, source redirection, Expo edits,
ZeroOne edits, scientific candidates or authority jobs. The 84-row repository blob
lock and its existing imports are read-only. New code may call immutable numerical
helpers explicitly; old entry points continue to use their original implementations.

Y2 remains `preseed_scientific_protocol_invalid`; AUTH1 unused, Strategy3 consumed,
topology-strategy budget zero, canonical-candidate budget one. All new evidence is
exposed project-authored development evidence, not physical/scientific qualification.

## Restart checkpoint

Current unit: C final evaluation. Unit A is draft PR67,
head `ac5900f6c3688225d22d6d60e766bb87e5a1d1d0`, tree
`ecd2d9388cfcb54d3042ca5a2f294c900956c131`.
Current branch: `codex/closy-forge-outfit-layer-runtime-integration-v1`.
Its exact parent is final B/PR68 head `795b8962d27cccf3da0d003b007d90cce4b4b12e`.
Worktree: `E:/apps/Closy-all-family-layer-integration-v1`.
Protected dirty primary worktree: `E:/apps/Closy` (unchanged).
Known failure reused: settled sleeve collapse, 2/4/2 simulation triangles becoming
8/16/8 dense triangles in long-sleeved top/button shirt/jacket. No export-face deletion.
Unit A evaluation completed: `.tmp/family-final-v2`, process exit 0, 54/54 builds,
27/27 deterministic pairs, 9/9 known-parameter capture/correction checks and
18/18 typed invalid-parameter rejections. Elapsed evaluator wall time: 1345.71 s.
The unchanged 8 mm paired-seam predicate passes 16/54 and fails 38/54 builds.
This is a mesh-validity improvement, not physical cloth convergence.

Published artifact index: `docs/evidence/family_integration_v1/publication_manifest.json`,
identity `5fd9f6b82e6d228cb83961ef9335de82f6179025e4895a6190a13c7f01504e4c`.
The publication verifies the relevant compiler import closure against the actual
start snapshot; unrelated B/C development is not misrepresented as a source freeze.
The three failed T-shirt integration attempts and two numerical prototypes remain
in the retained-development report. No failed row was replaced by a passing row.

Local receipts: schema check `fresh`, targeted Unit A/parser/publication tests
108 passed (exit 0; `.tmp/unit-a-targeted.xml`). Final CI belongs to the exact
pushed head; it is not inferred from these local receipts.
Post-evaluation static checking added list annotations to the evaluator only;
the executed compiler/capture implementation and runtime behavior are unchanged.
Unit B final evaluation `.tmp/binding-final-v2` exited 0: 18 packages, 9 deterministic
pairs, 99/99 baseline rows, 44/44 extra motion rows, three typed extra rejections,
17/17 baseline gates. Maximum rest error 5.809587714e-8 m, maximum motion error
0.016086708 m. Wall 68.24 s; CPU 65.69 s. All three Unit A binding API probes remain
unsupported (`binding_v2_unsupported_uv_lattice`), not global C3 passes.

Optional static run `E:/apps/closy-static-v3-02` exited 0: nine families pass actual
triangle/material/provenance/bounds audits. Z4/Z5/Z6/Z8 each pass 9; Z3/Z7 each
not_run 9. New read-only PR66 hash reuse receipt, no ZeroOne rebuild. First attempt
`.tmp/static-family-v3-01` retains nine processor cache path-length failures.

Unit C final outfit matrix `.tmp/outfit-final-v2` exited 0 (40 valid geometry rows,
zero fit-ready, 30 adjacent samples, 13 correct negative rejections, sources unchanged).
The first final attempt retains 17 completed quality failures
and an explicit interruption receipt: a separate no-contact lower-bound regression
required a reporting correction. Earlier `.tmp/outfit-probe-01` and `-02` remain
failed fit iterations. Security/report repairs: 110 focused tests passed. No fit
success is inferred from safe geometry or successful runtime transport.

The final demo has completed, with its failed outfit quality clearly labeled. The
final-code runtime44 using final outfit inputs also exited 0: all 44 rows, 22 pairs
and 43 controls pass, sourceFresh true. The final cumulative suite is still running.
No more than two measured heavyweight jobs were active. Publish the third draft for
required remote CI while this long local suite continues; explicitly retain pending
status. Final closeout waits for real local/remote outcomes rather than treating a
running process as passing. Saved evidence is losslessly archived with visible
summaries/images; full details remain available without hundreds-file witness churn.
See ACTIVE_BLUEPRINT_RESUME_OUTFIT_RUNTIME_V1.md for the latest safe pointer.

The reporting parser is a separate pure sidecar; its files do not alter frozen inventories.
