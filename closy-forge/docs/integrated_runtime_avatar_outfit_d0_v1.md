# Integrated Runtime, Avatar, and Outfit D0

This review unit integrates the historical Phase-12 runtime package, Phase-13 synthetic-avatar fit,
the accepted MT1 mechanical-reference namespace, C3 conventional deformation, and
LayerCollision-D0 behind one fail-closed headless decision surface.

## Runtime Contract

The conventional GLB remains mandatory and package validity never depends on ZeroOne. Candidate
static ZeroOne is selected only when package, fallback, and static derivative identities match. MT1
reference motion additionally requires exact topology, binding, request, output, and ZeroOne binary
identities. A mismatch, corrupt capability, unsupported version, or unavailable optional processor
falls back without weakening package validation.

MT1 is a clean analytic mechanical-reference profile. It is not solver-driven garment motion, Gate
Z2, PHY1, or product dynamic readiness. The runtime package carries qualified identity descriptors;
the actual static cook and dynamic output authority remains the immutable Phase-11 MT1 evidence.

## Avatar and Outfit Scope

The avatar path accepts only the exact project-authored synthetic collision-body authority and fit
digest. It contains no stable user identity, private capture, or licensed body evidence.

The outfit case executes an inner top and outer overshirt as indexed triangle surfaces derived from
the exact synthetic avatar rings. The simultaneous symmetric projection starts from a bounded
intersection/contact witness and records final clearance by shoulder/chest, chest, waist, and hem.
It preserves seam and opening inventories and verifies combined-surface intersections independently.
This is geometric `LayerCollision-D0`; it is not cloth simulation or visual-quality acceptance.

## Evidence

- `docs/evidence/integrated_runtime_avatar_outfit_v2.json` is the integrated execution record.
- `docs/evidence/canonical_outfit_surface_d0_v1.json` is the detached outfit surface witness.
- `docs/evidence/integrated_runtime_invalidation_ledger_d0_v1.json` calculates capability
  invalidation from exact identities.
- `docs/evidence/integrated_replay_manifest_d0_v1.json` disposes every replay source commit and
  proves PR26/PR31 business patches occur exactly once.

Host CPU latency and traced memory are advisory observations only. Mobile device, GPU, battery,
thermal, private-user, licensed-body, human-review, physical-cloth, and production evidence remain
`not_run` or false.
