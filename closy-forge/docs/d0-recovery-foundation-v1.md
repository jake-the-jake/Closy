# D0 Recovery Foundation v1

Unit L repairs recovery infrastructure without creating a fresh evaluator identity, target,
held-out pose, topology candidate, or physical attempt.

It separates immutable evidence anchors from finished publication heads, preserves Unit G's
failed benchmark and Unit H's consumed 0/8 C3 attempt, scopes the older C3 pass as legacy, and
records Unit I outcome M unchanged. The global physical-attempt index labels its oldest imported
record as historically unlinked rather than fabricating predecessor hashes. Seam-model budget is
zero and one final topology strategy remains; diagnosis does not consume it.

Evaluator v2 accepts declared mapping and list artifacts, retains immutable denominators, scores
all gate families independently, derives source/generated contribution, rejects target-derived
constants and framing, audits disjoint inventories, and mutation-tests semantic, validation, and
provenance fields. Its fixtures are generic and non-qualifying.

The contestant boundary uses a fresh allowlisted workspace, scrubbed environment, trusted audit
hook, confined output, separate evaluator lifecycle, and workspace destruction. On the current
Windows/application process path this is explicitly not an OS/container sandbox and cannot pass
D0-RP-04 isolation. A qualification pass requires enforced filesystem and network denial.

Strict C3 v5 defines one canonical frame schema, explicit versioned aliases, semantic seam and
opening metrics, topology/inversion/component checks, finite orthogonal frames, independent-process
determinism, whole-evaluation time/memory, and a candidate-independent analytic oracle boundary.
Its scope remains synthetic binding reconstruction, not physical or real-world ground truth.

The sentinel resolver now implements `Unit F else PR43`, reopens all required blobs including
`simulation/constraints.json`, and excludes Unit G candidates. All evidence is under
`docs/evidence/d0_recovery_foundation_v1/`; final Unit L head/CI attestation belongs in the draft
PR body to avoid recursive evidence.
