# Unit Y1 Publication

Unit Y1 ends with the literal outcome
`strategy3_dependency_blocked_before_seed_v3`.

The single final lock B was published at
`b9c6d34f4339fece2c6e6566b59c5b548f3f973a`. Its specialized authority workflow
is present at that head but absent from default branch `main`. GitHub therefore
returned HTTP 404 before creating the required external preflight run. The workflow
is `workflow_dispatch`-only, so no alternative immutable trigger existed.

This is a pre-seed infrastructure dependency block, not a scientific result. No
official seed, fixture, oracle, contestant output, or candidate was created. The
untouched Strategy-3 confirmation attempt and sole canonical candidate attempt remain
unconsumed. The prompt authorizes neither a replacement lock nor an automatic retry,
so Units Z, AA, and AB are ineligible and the sequence continues with unconditional
Unit AC.

Scientific source commit `d76916461d3e96b037fbc31b646319effef7a264`,
authority-wrapper source commit `f0c4580298f5fd56d2230d31f63d82b76d4a7df2`,
and lock B remain immutable. This publication adds derived status only.
