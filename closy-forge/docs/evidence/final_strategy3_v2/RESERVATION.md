# Unit U Strategy-3 Design Reservation

This commit reserves the sole remaining topology strategy before any Unit U implementation or
conformance execution. The fixed class is `seam-sequence-preserving conforming panel remesh with
duplicated seam degrees of freedom and rebuilt transfer/binding ancestry`, identified as
`PHY1-V5-S3-SEAM-SEQUENCE-CONFORMING-REMESH-V2`.

Topology budget state is `available=0, reserved=1, consumed=2, remaining=0`. The one canonical
candidate attempt remains available and unreserved. At most two public conformance-repair cycles
may make an implementation conform to these fixed equations; they cannot select another operator,
representation, correspondence parameterisation, transfer equation, or seam law.

No official holdout seed or instance exists, no canonical T-shirt is loaded or transformed, no
candidate is created, and no physical solver attempt is consumed by this reservation.
