# Unit T D0 T-shirt Confirmation v3

The exact-lock preflight and the single external authority execution ran in GitHub Actions run
`33621365050`. The official 256-bit seed was created once, so the study is consumed and cannot be
rerun. All 64 route attempts, 48 full-compile evaluations, 16 primary compile repeats, 24
appearance evaluations, and eight primary appearance repeats remained in their frozen
denominators. Sixty prediction artifacts completed successfully.

The literal result is `completed_benchmark_failed_absolute_gates`. D0-RP-04 passed; D0-RP-03,
D0-RP-06, and D0-RP-07 failed. No route was promoted. This is evidence only of disjointness from
the recoverable typed inventory; the relationship to the lost opaque v2 cohort remains unverified.

The authority claim is `procedural_freeze_and_container_isolation_not_cryptographic_secrecy`.
Commitments were written before prediction inside the authority job, but they became externally
downloadable only after the job completed. The imported artifact validates, the private authority
store is absent, and the workflow is sealed to verification-only execution.
