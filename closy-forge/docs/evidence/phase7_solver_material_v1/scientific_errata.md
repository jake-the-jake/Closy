# Phase 7 v1 append-only scientific errata

The frozen source labels include `xpbd` and `production_reference`; those display labels overstate the
implementation. The executed systems are two same-author, closely aligned, scalar one-dimensional
numerical chains. They have no 2D/3D specimen mesh, geometric distance/shear/bend constraints, XPBD
compliance or Lagrange multipliers, physical specimen dimensions, or unit-bearing applied loads.

Use the successor display labels `scalar_toy_coupon_forward_v1` and
`scalar_toy_coupon_inverse_reference_v1`. Frozen protocol identities and bytes remain unchanged.

The committed corpus exposed target fields before estimator authorship, and a unit test read a locked
row. The result is therefore retrospective and contaminated, not blind, locked, predictive, calibrated,
or scientifically single-use. The historical `predictiveNrmse` field is same-observation reconstruction
error. Confidence intervals were not run. Real coupon count is zero.
