# Active Blueprint Resume

This checkpoint is subordinate to `current_blueprint_status.json`, the committed predicate evidence,
and exact-head workflow results. Green CI proves only executed checks and does not promote failed or
not-run research predicates.

## Active Lane

- Repository: `jake-the-jake/Closy`.
- Branch: `codex/closy-forge-d0-truth-runtime-authority-v3`.
- Exact parent: draft PR #39 head `f732df267642cd55960205764e699c7fa2bb2d0f`.
- Immutable evidence source anchor: `01699163ed4470992f0e33ff9d9b6b4c65a67f7f`.
- Prospective review unit: draft PR #40; no merge is authorised.
- Final head and workflow URLs belong in the draft PR body, not a self-referential evidence commit.

## Unit A Result

- Matrix v2 evaluates 15 stable rows from opened artifact bytes and selected identity joins. It
  records 8 pass, 0 fail, and 7 not-run rows; Research Prototype remains partial.
- The first unmet row is exact decoded front/rear raster source identity. The new fixed-avatar
  neutral simulation row is also honestly not run.
- An explicit dependency graph joins source, capture, observations/corrections/cameras, fit,
  pattern/seams/openings, simulation/render topology, binding/fallback, appearance, optional
  derivatives, and runtime authority. Cycles, unknown nodes, duplicate authorities, private leaks,
  and cross-candidate evidence fail closed.
- Candidate runtime v2 parses authority from selected canonical package bytes and packages the
  canonical T-shirt garment fallback. It loads offline after source withdrawal.
- ZeroOne JSON entries are descriptors only; no descriptor is selectable as render/deformation data
  and no external execution is admitted without a separate exact execution authority.
- Runtime v1 and product selection remain unchanged. The new candidate is research-only.
- Raster and runtime decompression are bounded during streaming and reject truncation, trailing
  streams, declared-size mismatch, and high-ratio allocation abuse.

## Gate Table

| Gate | Scoped result | Exact boundary |
| --- | --- | --- |
| Research Prototype matrix v2 | partial | 8 pass, 7 not run; predicate-derived |
| C3-Binding-D0 v1 | pass | exact fixed-avatar public T-shirt identity |
| Z1 candidate static | scoped pass | descriptor identity remains non-payload |
| MT1 mechanical reference | pass | separate exact execution authority; not Z2 |
| PHY1-SingleLayer-D0-v2 | failed | historical 0/11 failure preserved |
| Z2 cloth-driven | failed/not admitted | no solver-driven cloth clip |
| P1 | not run | no private-user/product evidence |

## Privacy And Publication

- Public fixture hashes are committed only under explicit `public_fixture` classification.
- Future private qualification uses restricted handles and local envelopes; portable evidence may
  not contain raw/decoded source paths, hashes, or durable fingerprints.
- PR #39 exact head is externally attested by Forge run `33342673147`; committed status distinguishes
  its immutable source anchor from that published-head authority.

## Exact Next Action

After review unit A has exact-head green CI recorded in its draft PR body, create
`codex/closy-forge-d0-raster-identity-v2` from that exact published head. Commit the frozen public
front/rear and evaluator-only third raster identities before fitting, then execute source ingestion,
pixel-derived observations, correction replay, and all privacy/firewall controls. Do not append that
work here, run CCD, create solver-driven Z2, or promote the runtime candidate into product selection.
