# Active Blueprint Resume

This checkpoint is subordinate to `current_blueprint_status.json`, committed predicate evidence,
and exact-head workflow results. Green CI proves only executed checks and never promotes failed or
not-run research predicates.

## Active Lane

- Repository: `jake-the-jake/Closy`.
- Branch: `codex/closy-forge-d0-core-runtime-c3-v4`.
- Exact parent: draft PR #46 final head `069707bbd0bfc95eabbc5a3b3045e349d4c0b121`.
- Immutable Unit H evidence anchor: `3541507808946ae1248fba110b7732599db3fdbc`.
- PR #46 final head is externally attested by workflow `33470303559` with 29 of 29 Forge jobs.
- Unit H's draft PR #47 remains pending final exact-head attestation; its final head and workflow URL
  belong in its PR body rather than this self-referential checkpoint.

## Evidence-Integrity Reset

- Historical matrix v2 is preserved as `9 pass, 3 fail, 3 not-run`; it is superseded as current
  Research Prototype authority rather than rewritten.
- Matrix v3 opens the underlying evidence and reports core rows separately as `6 pass, 5 fail,
  0 not-run`. The first unmet required row is `D0-RP-03`.
- Supplemental rows independently report `2 pass, 0 fail, 2 not-run`; Z1 and MT1 cannot manufacture
  core Research Prototype status.
- `D0-RP-03` is `attempted_fail`, `D0-RP-04` is `attempted_integrity_error`, and `D0-RP-06` passes
  only for exact-fixture replay. Unit G attempted the identity-disjoint scope but did not complete
  canonical or appearance evaluation.
- The hash-chained attempt registry prevents deletion, mutation, tombstoning, or candidate changes
  from erasing failed history.

## Preserved Boundaries

- Original `D0-RP-07` remains failed: logo IoU `0.0` and displacement `0.154158086` against the
  frozen `0.14` maximum.
- Strict exact-candidate C3 and neutral simulation remain failed. Historical scoped C3 is preserved
  only under its original identity.
- The corrected PHY v4 diagnostic reopens the immutable 49-frame PR #43 trajectory. It does not
  rerun physics, consume/refund budget, or change `A_neutral_preflight_failed_v3`.
- Runtime v1 remains product-selected; topology v2 remains opt-in; the conventional garment fallback
  remains valid without ZeroOne.
- Two topology strategies remain and both seam-model trials remain consumed.

## Unit F Appearance Result

- Protocol commit `5a01ed40656c3f2924169f2c3e8f4d702f572cc5` locked one geometric
  source-surface-atlas strategy and one known-target replay before implementation.
- Implementation anchor `1038cbc9125461a8b80587b56e083191296861ec` was frozen before the
  source-only prediction commit `1dc8bec3adf8e9f35332b888499af8c2c0b8ae4c`.
- All eight anti-hardcoding controls pass. Active semantic islands are `0.552461694`
  source-observed and `0.447538306` explicitly generated controlled fill.
- The single known-target replay passes 34 of 34 atomic predicates and is labelled exactly
  `known_target_regression_pass`.
- This result is not held-out/cohort qualification. `D0-RP-07` remains failed, Research Prototype
  remains partial, and runtime v1 remains selected.

## Unit G Identity-Disjoint Result

- The complete lock committed 8 development identities, 16 evaluator slots, four contender routes,
  fixed isolation rules, one development-selected primary route, three compile routes, an 8-identity
  appearance subset, thresholds, penalties, and tie/promotion rules before evaluator realization.
- Seed-authority workflow `33467062432`, first non-rerun job `99729005374`, derived the sole evaluator
  seed. Sixteen salted target commitments were committed before 64 isolated predictions were frozen.
- Application-process deny-by-default isolation passed for every prediction: targets, third views,
  target parameters, the repository, `.git`, prior results, and network access were not mounted or read.
- After prediction freeze, all 16 target commitments validated. The frozen evaluator then failed once,
  before worker dispatch, because it required a JSON mapping while the committed raw draw transcript is
  a JSON list. The revealed cohort was not patched, replayed, replaced, shrunk, or rerun.
- The literal outcome is `benchmark_failed_fixed_inventory_unfinished`: 0 of 64 canonical compiles and
  0 of 32 appearance evaluations completed. No cohort metric, bootstrap, winner, promotion, or D0 row
  pass exists. Unit F's known-target result remains separate.

## Unit H Core Reproducibility and C3 Result

- H0 resolves only to Unit F candidate `candidate.d0_texture_rerender_v3.49161d8adafb514e5a04b1a9`
  with package digest `c338762915d390a22e879f588e0f01a4b6ed586b9ff045eb0a039888ace8223b`.
  Every required PR #43 pattern, seam, simulation/render topology, and binding input is byte-proven.
- H1 passes clean fresh, second clean, cache miss/hit, corruption invalidation, source withdrawal,
  and delete/rebuild checks for runtime digest `5a856b425b3a7e2139f774d1a2c2b9f7392a8b4bafb9e7418c51308c132b378e`.
- H2 and H3 remain not run: neither the exact authenticated static Z1 executable nor the clean MT1
  executable is available in the non-pinned dirty local ZeroOne checkout.
- H4 consumed its sole held-out attempt. The frozen evaluator entered `neutral` and failed before any
  state completed because it requested `maximumNormalLengthError` while the frame API exposes
  `maxNormalLengthError`. No patch, replay, metric result, C3 pass, PHY1 implication, or Z2 admission exists.

## Gate Table

| Gate | Scoped result | Exact boundary |
| --- | --- | --- |
| Research Prototype matrix v3 core | partial | 6 pass, 5 fail, 0 not-run; first unmet `D0-RP-03` |
| Matrix v3 supplemental | partial | 2 pass, 0 fail, 2 not-run; not core authority |
| D0-RP-06 exact fixture | pass | broader identity-disjoint scope is separately not run |
| Texture rerender known target v3 | engineering regression pass | 34/34 predicates; not qualification |
| D0 identity-disjoint T-shirt v1 | fixed-inventory benchmark fail | 64 predictions; 0 compiles; 0 appearance evaluations |
| Core Forge reproducibility H1 | pass, predecessor scoped | fresh/cache/corruption/withdrawal/delete-rebuild all pass |
| Exact static Z1 H2 / MT1 H3 | not run | exact authenticated processors unavailable |
| Strict C3 H4 | fail | one attempt consumed; 0/8 states completed after frozen adapter failure |
| C3-Binding-D0 v1 | historical scoped pass | preserved predecessor identity only |
| C3 exact candidate v2 | fail | no cross-identity repair |
| PHY1 seam/support neutral v3 | fail / outcome A | diagnostic rescore only; no rerun |
| Full PHY1 / CCD / solver-driven Z2 | not run | prerequisites failed |
| P1 | not run | no private-user/product evidence |

## Exact Next Action

After Unit H exact-head CI is green, create `codex/closy-forge-phy1-topology-strategy2-v1` from the
final Unit H head. Execute one bounded topology strategy under Unit I's pre-frozen classifier and
budget. Do not repair/replay Unit G or Unit H, do not donate predecessor C3 evidence, and create Unit J
only if Unit I emits an admissible canonical post-topology candidate; otherwise record logical J-A in I.
