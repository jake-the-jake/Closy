# Active Blueprint Resume

This checkpoint is subordinate to `current_blueprint_status.json`, committed predicate evidence,
and exact-head workflow results. Green CI proves only executed checks and never promotes failed or
not-run research predicates.

## Active Lane

- Repository: `jake-the-jake/Closy`.
- Branch: `codex/closy-forge-phy1-seam-support-v3`.
- Exact parent: draft PR #42 head `7922e9b6ece8fca2c3b7dec13299a39de102cbc4`.
- Immutable evidence anchor: `fe8f6d8a6d08e4c1b75a838728d66fea5d2c92c0`.
- Draft PR #43 remains unmerged; final head and workflow URLs belong in its PR body.

## Exact D0 Result

- Units A-C establish selected-byte runtime authority, exact public front/rear raster and
  observation lineage, deterministic source-conditioned fitting, and independent bitmap/PBR
  evaluation for candidate `candidate.d0_exact_fitted_topology_v2.060e8d4aaaa7e82eddb75880`.
- The final predicate matrix records 9 pass, 3 fail, and 3 not-run rows. Research Prototype remains
  partial; the first unmet required predicate is `D0-RP-07` source-texture rerender fidelity.
- `D0-RP-07` fails because logo displacement `0.154158086` exceeds the frozen `0.14` limit.
  `D0-RP-08` strict exact-candidate C3 and `D0-RP-15` neutral simulation also fail.
- Runtime v1 remains product-selected. Candidate runtime v2 is research-only, topology v2 remains
  opt-in, and the canonical garment GLB fallback remains available independently of ZeroOne.

## Physical Result

- The locked Unit D candidate is `physical.candidate.phy1_seam_support_v3.1152728eaf2588b4f674339f`.
  Its analytic suite passes all checks and all 11 corruption controls.
- The solver-active neutral trajectory has 49 frames. Junctions are non-vacuous and rank-aware,
  seams use zero spatial gap with separate crack/slip metrics, eight shoulder supports release by
  substep 16, and collision runs during iterations.
- Neutral outcome is exactly `A_neutral_preflight_failed_v3`: 11 of 28 checks pass. Independent
  scoring finds 242 unresolved contacts, `0.0024 m` residual depth, `0.041513220488 m` seam crack,
  `0.145067036152 m` tangential slip, and `-0.050591166989 m` dense-render clearance, plus support,
  terminal-motion, strain, area, shear, and runtime failures.
- A float32 reporting defect was independently reproduced and repaired without rerunning physics or
  changing any GLB byte. The 59-file evidence digest is
  `280df4684724d2dae73eb20a09008aec824c2c6476ed21325c754c9c05ef1b4c`.
- The stop rule applies: full 11-state PHY1, CCD, and solver-driven Z2 were not run. Two topology
  strategies remain; the second and final seam-model budget is consumed.

## Gate Table

| Gate | Scoped result | Exact boundary |
| --- | --- | --- |
| Research Prototype matrix v2 | partial | 9 pass, 3 fail, 3 not run; first unmet `D0-RP-07` |
| C3-Binding-D0 v1 | historical scoped pass | preserved exact predecessor identity |
| C3 exact candidate v2 | fail | not repaired with cross-identity evidence |
| PHY1 seam/support neutral v3 | fail / outcome A | 49-frame neutral only; stop rule applied |
| Full PHY1 v3 | not run | neutral prerequisite failed |
| Z1 candidate static | scoped pass | descriptor identity remains non-payload |
| MT1 mechanical reference | scoped pass | analytic transport only; not Z2 |
| Solver-driven Z2 | not run | exact C3 and PHY1 prerequisites failed |
| P1 | not run | no private-user/product evidence |

## Exact Next Action

After PR #43 has exact-head green CI recorded in its draft PR body, create
`codex/closy-forge-d0-texture-rerender-correction-v3` from that exact head. Precommit one
source-conditioned appearance correction whose literal acceptance includes logo displacement
`<= 0.14`, preserves the information firewall, and regenerates every affected candidate/package/
runtime/matrix identity. Do not spend topology strategy 2, run CCD/Z2, change the frozen PHY1-v3
trajectory, or promote runtime candidate v2.
